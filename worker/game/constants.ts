/** Time limit for answering each question (20 seconds) */
export const QUESTION_TIME_LIMIT_MS = 20000;

/** Cleanup delay after game ends or all disconnect (5 minutes) */
export const CLEANUP_DELAY_MS = 5 * 60 * 1000;
