import { HiddenInput, Input, Root } from './otp-field';

const OneTimePasswordField = {
	Root,
	Input,
	HiddenInput,
};

export { OneTimePasswordField };
export { Root as OTPField } from './otp-field';
