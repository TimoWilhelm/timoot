// Re-export from shared config - single source of truth
export { DEFAULT_BACKGROUND_IMAGES, getBackgroundImageById, getBackgroundImagePath } from '@shared/background-images';
export type { BackgroundImage } from '@shared/background-images';
